# =============================================
# Staging - Railway MySQL
# =============================================

spring.datasource.url=jdbc:mysql://shinkansen.proxy.rlwy.net:21406/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=xuausXXHyrQUiGJchxEPzadtixTBZJPB
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
gemini.api-key=${GEMINI_API_KEY}
gemini.model=gemini-2.5-flash
# Redis (override Redis en Railway, si no usa variables de entorno)
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}

# Frontend URL para staging
frontend.url=${FRONTEND_STAGING_URL:http://localhost:4200}
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:4200,http://127.0.0.1:4200}
